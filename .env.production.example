# PRODUCTION build-time vars for the Connect web app.
# These are inlined by Vite at BUILD time. In CI they are passed as Docker
# --build-arg values. Payments are turned OFF here (Pay Now + wallet top-up hidden).

VITE_API_BASE_URL=https://api.agrimarketconnect.shop
VITE_PAYMENTS_ENABLED=false

VITE_CLOUDINARY_CLOUD_NAME=your-cloud-name
VITE_CLOUDINARY_UPLOAD_PRESET=your-unsigned-preset
# Namespace browser uploads to the prod folder (matches API CLOUDINARY_FOLDER_PREFIX).
VITE_CLOUDINARY_FOLDER_PREFIX=agrimarket/prod
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-key
